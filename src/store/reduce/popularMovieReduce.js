const initialState = {
    movieItems: [],
}

const popularReduce = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default popularReduce;